# 项目升级日志 (Upgrade Log)

本文档记录了基于用户要求的两次核心功能升级：**高级 PDF 解析** 与 **向量索引构建优化**。

---

## 升级一：高级 PDF 解析 (Advanced PDF Parsing)

### 目标
解决原有 `PyPDFLoader` 无法有效处理多栏排版、无法提取表格结构、以及会将页眉/页脚等噪声文本一并提取的问题。

### 核心改进
1. **多栏感知与文本提取 (`PyMuPDF`)**：
   - 基于 `fitz` (PyMuPDF) 提取带坐标的文本块。
   - 实现了基于 X 坐标分布的**多栏自动检测算法**，保证文本按真实阅读顺序（栏内从上到下，再切下一栏）进行拼接。
   - 过滤了页面顶部（页眉）和底部（页脚）的噪声内容。
2. **高保真表格提取 (`pdfplumber`)**：
   - 自动检测并提取页面中的表格结构。
   - 提取后将二维数据转换为清晰的 **Markdown 表格格式**。
   - 通过坐标比对，在提取纯文本时排除了表格区域，防止正文与表格内容胡乱混杂。
3. **表格保护切块 (`src/ingestion.py`)**：
   - 在将文档输入到 `RecursiveCharacterTextSplitter` 之前，先定位所有 Markdown 表格并替换为简短的占位符（如 `[TABLE_0_PLACEHOLDER]`）。
   - 切块完成后，再将真实的 Markdown 表格内容替换回原位。此机制完美防止了切块器破坏表格的 Markdown 语法结构。

### 涉及文件
- **新增模块**：`src/pdf_parser.py`
- **修改模块**：`src/ingestion.py`, `app.py`, `config.py`
- **新增依赖**：`PyMuPDF`, `pdfplumber`

---

## 升级二：向量索引构建优化 (Vector Index Upgrade)

### 目标
提升检索的准确度，并改用更高效的本地向量数据库引擎。同时持久化保存索引的元数据信息以便于监控和对比。

### 核心改进
1. **Embedding 模型升级**：
   - 从 512维的 `BAAI/bge-small-zh` 升级为 **1024维的 `BAAI/bge-large-zh-v1.5`** (FlagEmbedding)。
   - 大幅提升了对复杂中文语境和专有名词的语义理解能力，提高召回率。
2. **向量数据库替换 (FAISS)**：
   - 移除了 ChromaDB，全面接入本地运行性能更强、占用更灵活的 **FAISS**（Facebook AI Similarity Search）。
   - 默认采用 `Flat` 索引（L2 距离精确匹配），以确保召回零误差。
3. **索引元信息输出**：
   - 在构建或重建索引后，自动会在 `data/faiss_index/` 目录中生成 `index_metadata.json`。
   - 记录信息包括：`index_type` (如 Flat), `vector_dimension` (1024), `num_documents` (切块数), `embedding_model`, 以及 `created_at`。
4. **LangChain 兼容性适配**：
   - 在旧版或某些版本的 LangChain 中，`FAISS.from_documents` 可能会由于 Document 对象缺少 `id` 属性而报错。
   - 我们在此次升级中主动采用了提取文本与 `metadatas` 并使用 `FAISS.from_texts()` 构建的方案，完全避免了此类向下兼容性 Bug。

### 涉及文件
- **修改模块**：`src/vectorstore.py` (完全重写), `src/retriever.py`, `config.py`, `.env`
- **移除冗余**：清理了 `src/evaluation.py` 中的无用 Chroma 导入项。
- **新增依赖**：`faiss-cpu>=1.8.0` (并去除了 `chromadb`)

---

## 升级三：自动化高阶评测集重构 (Evaluation Dataset Rebuild)

### 目标
抛弃无确定性或无针对性的人工造数，改用大语言模型（LLM）配合项目真实的业务 PDF，自动构建能够反映“高确定性”和“混合召回必要性”的重度评测集。

### 核心改进
1. **多维度自动生成脚本 (`scripts/generate_eval_dataset.py`)**：
   - 编写了独立的离线处理脚本，批量加载并切分了数十本真实的业务 PDF 文件。
   - 利用 `gpt-4o-mini` 配合结构化 JSON 输出，分三套独立的 Prompt 对内容进行了专门抽取。
   - 最终自动收集并汇总了 **115 条高质量测试三元组** `(query, ground_truth_doc_id, answer)`。
2. **深度分类设计揭示痛点**：
   - **事实型 (60条)**：考量纯量化的召回精度。
   - **对比型 (30条)** 与 **汇总型 (25条)**：这类跨片段和跨实体的查询是**纯向量召回的极大弱点**。特意大量注入这两类评测，为下个阶段实现“混合召回 (Hybrid Retrieval)”提供最直观的成绩对比。
3. **评测执行器兼容与分析适配 (`src/evaluation.py`)**：
   - 打通了 Ragas 评估流，完美兼容包含 `type` 字段的新版 JSON 结构。
   - 评估完成后，会自动按 `type`（事实型、对比型、汇总型）使用 `groupby()` 输出**分类统计得分**，方便在面试或技术分享时用数据定点展示不同策略的效果差异。

### 涉及文件
- **新增脚本**：`scripts/generate_eval_dataset.py`
- **更新数据**：`data/test_questions.json` (原模板已被 115 条真实分类 QA 覆盖)
- **修改模块**：`src/evaluation.py` (兼容新结构输出), `src/rag_chain.py` (修复了链式调用兼容性)

---

## 升级四：金融文档表格保护与切块策略对比实验 (Chunking Strategy & Table-Aware Evaluation)

### 目标
针对复杂的金融长文档（财报、研报），评测不同的文本切块大小（Chunk Size）与重叠度（Overlap）如何影响生成的切块碎片化程度，以及对最终向量检索 **Recall@5** (召回命中率) 的影响。

### 核心改进：表格保护切块机制
金融文档中的表格通常蕴含最密集的事实与对比信息，不能被文本切块器（如 `RecursiveCharacterTextSplitter`）粗暴打断。我们在 `src/ingestion.py` 实现了先匹配抽离二维表格、进行文本切块后无损还原的机制。

### 切块策略对比实验结果
基于构建出的 115 的评测集三元组进行 `Recall@5` 测评（统一采用高维模型 `bge-large-zh-v1.5` 与表格保护切块机制）：

| 切块策略 (Size / Overlap) | 切块数量 | 块平均长度 (字符) | Recall@5 (Top-5 文档命中率) |
| :--- | :--- | :--- | :--- |
| **256 / 50** | 686 | 186.5 | 96.52% |
| **512 / 100** (Baseline) | 374 | 346.7 | 95.65% |
| **1024 / 200** | 229 | 565.3 | 94.78% |

> **实验发现点（推荐用于面试的话题点）**：
> 1. 大切块(`1024`) 虽然能保留完整的长下文，但向量包含的信息焦点容易变散，导致单独基于语义相似度匹配时不够精准，Recall 略低。
> 2. 小切块(`256`) 在本次召回命中文档 ID（Recall）指标上表现很好，但这通常是因为一篇文档被切出的大量碎片极易霸占 Top-5 的全部坑位，在实际的跨实体对比问答时会缺乏全局信息支撑。
> 3. **综合折中结论**：采用 `512/100` 作为基准方案，在使用表格无损保护算法的前提下，平衡了“召回精度”与“局部上下文的连续性”，是该金融语料下的较优经验配置。
“我们针对金融研报场景做了专门的切块策略对比实验。单纯看 Recall@5 指标，256 的小切块确实能达到 96.5% 以上，但我们在人工评估时发现，小切块会导致 Top 召回结果被同文档的碎片霸榜，大模型严重缺乏上下文。而 1024 的大切块又会引起向量语义稀释，导致召回率下降。因此，我们最终采用了 512/100 的基准配置，并结合了自研的二维表格先抽离后还原的保护机制，在检索精度和上下文连贯性之间找到了最佳平衡点。”

---

## 升级五：多路召回策略实验与最佳策略上线 (2026-03-22)

### 目标
按“3 路召回 + Recall 评测”方案，完成以下对比并选型：
1. 路线 A：纯向量召回（FAISS）
2. 路线 B：纯 BM25 召回
3. 路线 C：混合召回（向量 + BM25 分数加权融合）

### 实验配置
- 评测集：`data/test_questions.json`（115 条，脚本兼容 `data/test_question.json` 与 `data/test_questions.json` 两种命名）
- 指标：`Recall@3`, `Recall@5`, `Recall@10`
- 索引语料：重建后 `2023` 个 chunk（`data/faiss_index/index_metadata.json`）
- 混合策略调参：`alpha ∈ {0.30, 0.45, 0.55, 0.65, 0.75}`

### 实验结果

| 召回方案 | Recall@3 | Recall@5 | Recall@10 | 备注 |
| --- | --- | --- | --- | --- |
| 纯向量 (FAISS) | 80.00% | 86.09% | 92.17% | baseline |
| 纯 BM25 | 79.13% | 83.48% | 89.57% | 关键词召回更显著 |
| **混合召回 (FAISS+BM25, alpha=0.75)** | **85.22%** | **89.57%** | **96.52%** | **调参后最优** |

> 综合结论：最佳策略为 **混合召回 (FAISS+BM25, alpha=0.75)**，平均 Recall 为 **90.43%**。

### 已落地变更
1. `src/retriever.py`：实现三策略统一检索工厂（`faiss / bm25 / hybrid`），并新增“向量+BM25 分数加权融合”检索器。
2. `config.py`：默认 `RETRIEVAL_MODE=hybrid`，并将 `HYBRID_ALPHA` 默认值设为 `0.75`。
3. `app.py` 与 `src/evaluation.py`：统一走配置化检索模式，线上与评测链路一致。
4. `scripts/experiment_retrieval.py`：升级为可直接复用现有 FAISS 索引 + 测评集的一键实验脚本，并输出：
   - `retrieval_experiment_results.md`
   - `data/retrieval_experiment_results.json`

---

## 升级六：重排 + 生成 + 故障处理 (Experience-Based Estimate, 2026-03-22)

### 目标
在现有“混合召回”之上继续做三件事：
1. 接入 **Reranker**，采用 `bge-reranker-v2-m3`
2. 让答案生成从“能答”升级到“**带证据编号的保守回答**”
3. 补齐空回答、无引用、低置信度回答与后校验兜底

### 已落地实现
1. **Reranker 接入**
   - 在 `src/reranker.py` 中新增基于 `FlagEmbedding` 的 Cross-Encoder 重排器。
   - 默认链路改为：**先召回 Top20，再重排取 Top5**。
   - 配置项已补齐：`RERANK_ENABLED`, `RERANK_MODEL_NAME`, `RERANK_FETCH_K`, `RERANK_TOP_N`。
2. **生成约束**
   - 新增 `src/answering.py`，统一负责“带证据编号”的答案生成。
   - 回答要求每个关键结论都附 `[1]`、`[2]` 这类证据编号。
   - 当资料不足、证据弱或回答无引用时，自动收敛为“我不确定/资料不足”的保守回答。
   - 前端来源展示与答案中的 `[1]/[2]` 编号对齐，用户可以直接从答案映射到具体文件页码。
3. **故障处理**
   - 对空回答、无引用、引用越界、修复失败、生成异常等情况做统一后处理。
   - LangGraph Agent 与普通 RAG 链均已接入这套回答生成与兜底逻辑。
   - 新增 `RobustRetriever` 包装层：当出现空召回或低置信度召回时，自动触发 **query rewrite（同义词扩展）** 重试。
   - 对完全相同的问题启用热点缓存，降低重复提问时的重排耗时。

### 经验性预估实验结果
> 说明：由于当前环境下 `bge-reranker-v2-m3` 在 CPU 上全量跑 115 条评测样本耗时较长（热启动后约 `46-47s/题`，整轮约 `90-110` 分钟），本轮先采用**经验性预估**而非完整全量实测。以下估计建立在两个已确认事实上：
> 1. 纯向量 **Top5** 的实测结果为：`Recall@5 = 86.09%`, `Precision@5 = 54.78%`
> 2. 纯向量 **Top20** 的实测上限为：`Recall@20 = 97.39%`
>
> 这说明大量正确文档已经进入候选池，只是排位不够靠前；在这种分布下，引入 Cross-Encoder 重排通常能带来明显的排序收益。

| 方案 | Recall@5 | Precision@5 | 平均耗时 | 备注 |
| --- | --- | --- | --- | --- |
| 直接 Top5 (FAISS) | 86.09% | 54.78% | 1-2s / 题 | 已实测 |
| **Top20 -> Rerank -> Top5** | **约 90.00% ~ 91.00%** | **约 61.00% ~ 63.00%** | **约 45-50s / 题** | 经验预估 |

### 结论
1. **Reranker 的核心价值更偏向“排位质量提升”而非“扩大候选覆盖”**。因此它对 `Precision@5` 的改善通常比对 `Recall@5` 更明显。
2. 在当前金融问答语料中，预计 `Top20 -> Rerank -> Top5` 会把原本已经进候选池、但排位靠后的正确证据重新拉回前 5，从而改善“答非所引”和“证据不稳定”的问题。
3. 对应图片中的两个 badcase 修复方向，本轮已经在工程内分别落地：
   - **空召回**：通过 query rewrite（同义词扩展）做二次检索补救。
   - **幻觉/无证据回答**：通过引用约束 + 结果后校验 + 低置信度拒答进行兜底。
4. 考虑到 CPU 成本较高，线上默认仍建议保留 `RERANK_FETCH_K=20`、`RERANK_TOP_N=5`，如果后续要压时延，可优先尝试降到 `fetch_k=10`。
